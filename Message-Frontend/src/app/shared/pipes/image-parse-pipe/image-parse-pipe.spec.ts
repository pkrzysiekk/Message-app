import { ImageParsePipe } from './image-parse-pipe';

describe('ImageParsePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageParsePipe();
    expect(pipe).toBeTruthy();
  });
});
