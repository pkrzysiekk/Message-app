import { ClickedOutside } from './clicked-outside';

describe('ClickedOutside', () => {
  it('should create an instance', () => {
    const directive = new ClickedOutside();
    expect(directive).toBeTruthy();
  });
});
